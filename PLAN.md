# connectome-gen — 뇌지도 생성 AI (NOMS-LAB)

> 뇌지도(connectome)를 학습해 새로운 뇌지도를 생성하고, 그것이 "작동"하는지 검증하는 프로젝트.

## 저장 구조 (C/D 분할)
- **코드·가공텐서·체크포인트** → `C:\Users\yuulf\Desktop\NOMS-LAB-C\connectome-gen\` (SSD)
- **원본 데이터·출력물·로그** → `D:\NOMS-LAB-D\connectome-gen\` (대용량)
  - 원본 repo: `D:\NOMS-LAB-D\connectome-gen\data\raw\nature2021\`

## 목표
1. **생성**: 기존 connectome을 학습해, 학습하지 않은 *새로운* connectome 그래프를 생성하는 AI
2. **검증**: 생성된 connectome이 (1) 구조적으로 유효한지, (2) 기능적으로 작동하는지 평가

## 대상 생물: C. elegans (예쁜꼬마선충)
- 302 뉴런 — RTX 3080에서 가볍게 학습 가능
- 가장 잘 연구된 connectome (완전 매핑)
- **결정적 이유**: OpenWorm/c302 라는 기능 시뮬레이터가 존재 → "작동" 검증 가능
  - 초파리(14만)·생쥐는 통째 기능 시뮬이 없어 검증 단계 불가

## 데이터
- **Witvliet et al. 2021 (Nature)** — 발달 8단계 connectome (birth→adult). 생성 모델 학습용 핵심 (같은 종의 변이 샘플)
- **Cook et al. 2019** — 암/수 성체 완전 배선도 (wormwiring.org)
- **Varshney et al. 2011** — 고전 connectome (네트워크 분석용 baseline)
- **OpenWorm c302** — 기능 시뮬레이터 (검증용)

## Phase 1 — 생성
- 입력 표현: 인접행렬(가중/방향) + 뉴런 메타(위치 3D, 타입: sensory/inter/motor, 좌우)
- 모델 후보:
  - **Graph VAE** — 잠재공간 샘플링으로 새 그래프
  - **Graph diffusion** — 더 강력, 구현 복잡
  - **Generative wiring rules (Betzel류)** — baseline, 공간+위상 규칙 ~13개 파라미터
- 출력: 학습 분포에서 나온 *새로운* connectome

## Phase 2 — 검증 ("작동하냐")
### 1. 구조적 유효성 (빠름/정량)
- 차수분포, motif 빈도, small-worldness, 모듈성, 좌우대칭성
- 실제 벌레 통계와 매칭되는가
### 2. 기능적 유효성 (진짜 "작동")
- 생성 connectome을 c302 동역학 시뮬에 주입
- 감각자극 → 운동뉴런 반응 → locomotion 패턴(전진/후진/방향전환) 발현 여부

## 핵심 난제 = 연구 기여
**새로움 vs 유효성 트레이드오프**
- 복붙하면 유효하지만 무의미, 너무 새로우면 작동 안 함
- "새로운데 작동하는" connectome 생성 = 핵심 가치
- novelty 지표(학습셋과의 그래프 거리) vs 기능 점수의 파레토 곡선을 보여주는 것이 목표

## 공개 계획
- GitHub repo (코드 + 생성 샘플 + 평가 파이프라인)
- 결과 좋으면 워크샵 페이퍼 / 블로그

## 진행 로그
- 2026-06-19: LAB 생성, 계획 고정, C. elegans 타깃 확정.
- 2026-06-19: **Phase 0 완료** — Witvliet 8단계 데이터 수집 + 로더(`src/load_connectome.py`).
  텐서화 완료: 229노드(union), chem/gap 인접행렬, 3D좌표, 발달단계별 존재마스크 → `data/processed/celegans_dev.npz`.
  성장곡선 재현: 뉴런 187→221(+18%), 화학시냅스 1296→7970(+515%) = 발달 = 시냅스 densification.
- 2026-06-19: 저장소 C/D 분할 정리.
- 2026-06-19: **Phase 1 v1 완료** — 엣지 단위 생성 모델 (`src/phase1_edge_model.py`).
  엣지=샘플 프레이밍으로 312,574개 학습신호 확보(8개 그래프 벽 우회).
  예측: 단계1~7 학습 → 성체(8) 엣지 AUC **0.781** (거리 baseline 0.729 능가) = 진짜 배선규칙 학습.
  생성(밀도맞춤): density/평균차수/**sensory→inter→motor 정보흐름** 재현 성공.
  한계: **상호성 0.279→0.053**, 허브 약간 부족 — 엣지 독립샘플링의 구조적 한계.
  → v2 과제: 노드 잠재임베딩(허브) + 엣지 의존성/상호성 모델링.
- 2026-06-19: **Phase 1 v2 완료** — 노드임베딩+상호성, GPU전용/벡터화 (`src/phase1_v2.py`).
  logit = MLP(type,dist,stage) + u_i·v_j(허브) + s_i·s_j(상호성).
  예측 AUC **0.78→0.96**, AP **0.15→0.71**. 생성: 상호성 0.05→**0.22**(실제0.28), 허브 일치(33/35 vs 32/39).
  → C.elegans 구조생성 **토대 완성**. 다음: (a)초파리 유충 스케일 (b)작동검증(c302).
  CPU는 타 프로젝트(NOTP) 점유 → GPU 전용으로 진행. 파이프라인 벡터화로 스케일 대비 완료.
- 2026-06-19: **Phase 2A 완료** — 싼 작동검증 프록시 (`src/phase2a_function_proxy.py`).
  감각자극→선형확산→운동활성. 운동뉴런 활성패턴 상관: 생성 **0.379** vs 무작위 **-0.168**.
  운동 도달 100%, 감각→운동 평균 1홉. → 생성 뇌가 기능적으로도 우연 이상으로 실제 닮음.
  한계: 부호(흥분/억제) 미반영·선형 → 생물물리는 B(c302).
- 2026-06-19: **Phase 2B-1 완료** — GPU LIF 스파이킹 시뮬 (`src/phase2b_lif.py`).
  Shiu 2024식 LIF + Dale 부호. 운동 발화패턴 상관: 생성 **0.509** vs 무작위 -0.157.
  (A의 0.38보다↑) caveat: 발화율 포화·C.elegans는 graded라 LIF는 근사.
- 2026-06-19: **B2 c302 준비완료** — c302 0.12.0+pyneuroml 설치, 커스텀 리더(`src/c302_gen_reader.py`)
  로 생성/실제 connectome을 c302에 주입(212뉴런/2159연결). **Java(OpenJDK17) 설치 대기 중** → 들어오면 시뮬.
- 2026-06-19: **대규모 GPU 파이프라인 완료** (`src/gpu_pipeline.py` + `make_pipeline_input.py`).
  negative sampling(양성+무작위음성), N^2 미생성 → 초파리 14만뉴런 대비. 벌레 검증 AUC 0.84 / 0.4초.
  초파리: 같은 포맷 npz 만들어 경로만 넘기면 동작.
- 2026-06-19: **B2 c302 실행완료(결과 inconclusive)** — `src/phase2b2_c302.py`+`phase2b2_compare.py`.
  OpenJDK17 설치, c302 NeuroML 생성 + jNeuroML 시뮬 전과정 작동(180뉴런, 200ms ~18초/모드).
  BUT 기본 param(C)+감각자극으로 **전 뉴런 활성 동일(0.00248)** = 자극 전파 안 됨 → 판별 불가.
  c302는 입력전류·시냅스게인 손튜닝 필요(생물물리 sim의 난점). **작동검증 신뢰결과는 A·B1.**
  종합 상관(생성/무작위 vs 실제): A 0.38/-0.17, B1 0.51/-0.16, B2 degenerate(튜닝 필요).
- 2026-06-19: **Phase 3 완료 — 기능 최적화 닫힌루프** (`phase3_functional_opt.py` RL / `phase3_es.py` ES).
  엣지 logits를 LIF 기능상관 보상으로 최적화(밀도고정+KL, 새로움 모니터).
  **RL(REINFORCE)**: 기능상관 0.39→**0.67**, 실제겹침 0.575→0.579(새로움 유지). 24샘플×150it.
  **ES(OpenAI-ES)**: 인구40 약함(0.43), 인구256+Adam→**0.67**(RL동률), 겹침 0.575→0.569.
  결론: 둘 다 "새로운데 더 작동하는" 뇌 생성 성공. RL=샘플효율(해석적 grad), ES=견고/병렬(인구↑필요).
  **핵심 입증: 기능을 목적으로 생성기를 최적화하면 작동성이 0.39→0.67로 상승, 새로움 손실 없음.**
- 2026-06-19: **Phase 3B 완료 — 강건성/일반화** (`phase3b_robust.py`).
  자극 12조건 중 8개로만 최적화 → 미학습 4개 조건에서도 기능상관 0.435→**0.574**(무작위 -0.01).
  train(0.579)≈test(0.574) = 과적합 제로, 새로움 0.578 유지. → 단일조건 트릭 아닌 일반 라우팅 충실도.
- 2026-06-19: **단계 IV 완료 — c302 골드스탠다드 확인 (B2 청산)**.
  범인=param C 기본 자극전류 0pA. 살리고 subthreshold graded 영역 튜닝(STIM 4pA, gbase 0.3nS), 지표=평균탈분극.
  **c302 생물물리 시뮬: 생성 vs 실제 0.777, 무작위 -0.014.** 우리 LIF(0.51)보다 높음(graded가 벌레 생리에 더 적합).
  → 세 독립 시뮬(A 0.38/B1 0.51/B2 0.78) 전부 "생성≈실제>>무작위" 일치. 작동성 = 아티팩트 아님, 철벽.
- 2026-06-19: **단계 V 시작 — 종간 일반화 (초파리 유충)**. Winding 2023 데이터 확보
  (`make_pipeline_input_fly.py`), 2952뉴런/110k엣지/18타입. `gpu_pipeline.py` 타입수 자동인식 패치.
  **같은 negative-sampling 파이프라인이 초파리에서 test AUC 0.818 (벌레 0.84급), 3.7초.**
  → 생성 방법이 벌레 전용 아님, 곤충 뇌도 배선규칙 학습. 성체 FlyWire(14만) 스케일 경로 검증.
- 2026-06-19: **단계 V-생성 완료** (`phase5_fly_generate.py`). 초파리 connectome 전수생성+구조검증.
  밀도·평균차수 일치, 허브 부분포착, **세포타입간 흐름(18x18) 실제와 상관 0.983**(무작위 0.40).
  상호성 약함(0.04 vs 0.26): 파이프라인 모델이 worm v2보다 단순(초파리 위치정보 없음)+덜튜닝.
  → 종간 생성 일반성 입증: 곤충 뇌 정보 아키텍처를 거의 정확히 생성.
- 2026-06-19: **단계 VI 완료 — 포유류급 생성 스케일 (`phase6_scale.py`)**. 산출물·모델 D 저장.
  핵심: 노드임베딩 버리고 **배선규칙(타입+3D거리)** 만 학습 → 임의 스케일 적용 + **희소생성(N² 미생성)**.
  **RTX 3080 한 장으로 1.5억 뉴런(쥐 전뇌 2배) 생성, 3.6GB, OOM 없음.** 70M=쥐전뇌급 110초.
  한계: (1)엣지수 ~2.2M 정체(무작위후보 비효율 → 공간국소 샘플러로 수정가능) (2)실제 포유류 커넥톰 없어 사실성 미검증.
  = 생성 기계의 포유류급 뉴런수 확장 **공학적 실현성 증명**(사실성≠).
- 2026-06-19: **단계 VII — 새로움 vs 충실도 정량화** (`phase7_novelty_fidelity.py`).
  유저질문 "학습한것과 얼마나 다른가" 답: 생성 vs 실제 Jaccard **0.405**(=60% 새로 만듦).
  맥락: 무작위 0.024 << 생성 0.405 ≈ **자연변이(실제 벌레7vs8) 0.512**. 생성샘플 다양성 0.48.
  → **베끼기 아님·무작위 아님, 자연변이 수준의 새로움.** 충실도: 타입흐름 0.99, 기능 c302 0.78.
  한줄: "실제와 60% 다른데(자연변이 수준), 구조·기능은 실제 재현".
- 2026-06-19: **#2 해결 — 공간-국소 샘플러** (`phase6_scale.py` scale_generate_local).
  같은 3D셀서 후보 샘플 → 평균차수 14.6 **고정**(N무관) = 엣지 선형. 쥐 전뇌급 7천만뉴런서 **10.2억 엣지** 38초/8GB.
  → 대규모 생성이 생물학적 밀도로 가능. 엣지정체 완전 해결.
- 2026-06-19: **초파리 작동검증** (`phase5b_fly_function.py`). 감각자극→LIF→하행출력(DN) 발화패턴.
  생성 vs 실제 **0.492**, 무작위 0.024. → "작동하나"가 2종 확인(벌레 c302 0.78 + 초파리 DN 0.49).
- 2026-06-19: **단계 VIII — 행동/본능 분석** (`phase8_behavior_instinct.py`).
  본능 테스트=터치-회피 반사회로(앞터치 ALM/AVM→후진명령 AVA/AVD). 구조경로비율[후진/(후진+전진)]:
  실제 0.602(회피회로 존재) vs 생성 0.522(편향 소실, 무작위 0.582보다도↓). 뒤터치(PLM)는 데이터에 없음.
  → **특정 본능회로 보존 안 됨.** 생성=통계적 배선규칙 → *집단행동*은 재현하나 *정밀 반사배선(본능)*은 못 따라옴.
  결론: 생성 뇌 = 실제같은 정보아키텍처(작동O)지만 하드와이어 본능 없음 = "다른 무언가"(통계적 뇌).
- 선행연구: generative connectome models 존재(Betzel2016 기하+위상, 2024 축삭성장) = 구조생성은 기존.
  단 기능검증·기능최적화(RL/ES)·본능분석은 직접 선례 안 보임 → 우리 차별점(기능적 측면).
- 2026-06-19: **단계 IX — 실제 포유류 사실성 검증 (MICrONS 쥐 피질)**. CAVE 토큰 발급+약관동의,
  검증된 2220뉴런/183k엣지/12타입(실제 위치 포함) 다운로드(`pull_microns.py`). 학습 AUC 0.858.
  생성+구조비교(`phase9_realism.py`): 밀도·평균차수 정확, **세포타입흐름 상관 0.991**(무작위0.56),
  차수분포 cos 0.62, 상호성 0.13(실제0.27), 허브 407(실제785), **새로움 91%(겹침0.09)**.
  → **생성 쥐 뇌가 피질 세포타입 배선 아키텍처를 거의 완벽 재현**, 허브·상호성은 부분. 91% 새로움.
  교차종 패턴: 타입흐름은 항상 ~0.99(벌레0.99/초파리0.98/쥐0.99) 재현, 허브·상호성은 항상 부분.
  CAVE 토큰 caveclient 로컬 저장됨(재pull 가능).
- 2026-06-19: **단계 X — 학습 가능성** (`phase10_learning.py`). connectome=reservoir 구조로 계산/기억능력 측정.
  벌레: 생성 18.4 ≈ 실제 17.9(선형기억), 비선형 0.146≈0.153 → **생성=실제 학습능력**. 쥐: 실제 낮음(극단허브가 동역학지배), 생성↑.
  둘다 무작위 못 이김(생물망은 범용기억 최적화 아님). 핵심=벌레서 생성≈실제 깔끔 확인.
- 2026-06-19: **단계 XI — 시각화** (`phase11_viz.py`). D/outputs/large/viz/ 5개 PNG.
  벌레 인접행렬·쥐 타입흐름: 실제 vs 생성 눈으로 거의 동일. 쥐 위치=실제 피질 층구조 확인.
- 2026-06-19: **단계 XII — 학습보다 큰 뇌 + "어떻게 만드나"** (`phase12_bigger.py`).
  쥐 2220뉴런 학습 → 규칙으로 **50,000뉴런(23배) 생성, 0.2초, 306만엣지**. 타입흐름 상관 0.762(23배 외삽).
  **AI가 배운 2규칙 시각화(`viz/6_bigger_brain_rules.png`)**: ①거리커널=가까울수록 연결(P 0.52→0.05 감쇠),
  ②세포타입 친화도=억제뉴런(BC/MC)→피라미드세포 강한연결(피질 억제문법). 뇌 생성=뉴런 늘리고 이 규칙 적용.
  → AI는 특정 뇌를 외우는 게 아니라 **생물의 실제 배선원리(공간+세포타입)를 학습**해서 임의 스케일 적용.
- 2026-06-19: **단계 XIII — 큰 뇌 vs 작은 뇌 (차이+학습능력)** (`phase13_scale_compare.py`).
  구조차이(견고, 3회 일관): **밀도가 크기 따라 급감**(500뉴런 0.088→8000뉴런 0.007, 13배 희소),
  평균차수 일정(~50), 높은 국소 군집(~0.4-0.6). = 큰 뇌일수록 더 희소·모듈러(국소적), 실제 생물과 동일.
  학습능력: **결론 못 냄(정직)** — reservoir 용량 측정이 크기마다 하이퍼파라미터(스펙트럼반경)에 과민,
  rho 0.95=큰뇌서 포화붕괴 / 0.6=전체 감소. 안정적 트렌드 안 나옴. 이론상 뉴런↑=잠재용량↑이나 자동실현 아님.
  → 통찰: **규칙을 그냥 키운다고 더 똑똑해지지 않음.** 큰 뇌=더 희소·모듈러지만 계산능력 자동증가 아님.
- 2026-06-19: **단계 XIV — 정밀구조 정복 (허브+상호성)** (`phase14_fine_structure.py`). 쥐.
  프로젝트 내내 못 잡던 두 가지 직접 공략:
  ① **차수 교정**(노드별 out-degree=실제 목표, 행별 바이어스) → **허브 407→778(실제 785), 차수std 109.8(실제 109.7) 완벽**.
  ② **상호성 교정**(비상호 엣지에 reverse 주입+밀도유지) → **상호성 0.13→0.32(실제 0.27) 정복**.
  둘 다 적용: 허브 699/상호성 0.325 (둘 교정 간 약간 간섭, 정제가능). 임베딩 키우기(EMB32)만으론 허브 안 잡힘.
  → **이제 생성기가 실제 connectome의 *전체 구조 프로파일*(타입흐름+허브+상호성+밀도) 재현 가능.** 정밀구조 산 등정 시작.
  caveat: 차수열·상호성 목표를 파라미터로 씀(degree-corrected 모델 관례, 합법) — 개별 배선은 여전히 새로움.
- 2026-06-19: **단계 XV — 본능회로 보존** (`phase15_instinct.py`). 벌레 터치-회피 회로.
  규칙전용 vs 임베딩 모델 비교. **임베딩이 실제 회로엣지(앞터치 ALM/AVM→명령뉴런)에 ~9배 높은 확률**(0.40-0.47 vs 평균 0.048),
  규칙전용은 ~2배. → **임베딩은 본능회로의 *특정 배선*을 포착(개별 정체성 학습).**
  단 경로비율(escape 방향성) 생성서 0.527 vs 실제 0.602 — **회로 배선은 잡으나 방향 비대칭(후진우세)은 미완.**
  결론: 본능 보존 부분 진전. 배선 존재는 임베딩이 잡고, 완전한 반사 기능엔 가중치/부호 등 더 정밀한 모델 필요.
